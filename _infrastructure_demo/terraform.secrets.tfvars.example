# Copy this file to terraform.secrets.tfvars and fill in your real values.
# This file is intentionally NOT used by default unless you pass -var-file
# or use the provided x-plan.sh/x-apply.sh helpers which detect it.

subscription_id = "00000000-0000-0000-0000-000000000000"  # REQUIRED

# You may also move sensitive secrets here instead of committing them in terraform.tfvars.
# Example:
# secrets = {
#   apiKey       = "..."
#   connection   = "..."
#   webhookToken = "..."
# }
